# Docling Document Parsing Service

Веб-сервис для парсинга документов на основе [Docling](https://github.com/docling-project/docling) от IBM Research Zurich.

## Возможности

- 🗂️ Парсинг множества форматов документов
- 📑 Продвинутое понимание PDF (структура, таблицы, формулы, код)
- 🧬 Единый формат представления DoclingDocument
- ↪️ Экспорт в Markdown, HTML, JSON
- 🔒 Локальное выполнение (без отправки данных в облако)
- 🔍 OCR для сканированных PDF и изображений
- 🤖 Поддержка Visual Language Models (GraniteDocling)
- 🎙️ ASR для аудио файлов
- ⚙️ Гибкая конфигурация через YAML
- 🐳 Docker и Docker Compose поддержка

## Поддерживаемые форматы

### Входные форматы

**Документы:**
- PDF (включая сканированные с OCR)
- DOCX (Microsoft Word)
- PPTX (Microsoft PowerPoint)
- XLSX (Microsoft Excel)
- HTML

**Аудио:**
- WAV
- MP3
- VTT (Web Video Text Tracks)

**Изображения:**
- PNG
- TIFF
- JPEG
- И другие форматы изображений

### Выходные форматы

- **Markdown** - структурированный текстовый формат
- **JSON** - структурированные данные с метаданными
- **HTML** - веб-представление документа

## Установка и запуск

### Локальный запуск

1. **Установка зависимостей:**
```bash
cd docling_exp
pip install -r requirements.txt
```

2. **Настройка конфигурации:**
Отредактируйте `config.yaml` при необходимости (см. раздел "Конфигурация")

3. **Запуск сервиса:**
```bash
python run.py
```

Сервис будет доступен по адресу: http://localhost:8000

### Docker Compose (рекомендуется)

1. **Настройка конфигурации:**
Отредактируйте `config.yaml` и при необходимости `docker-compose.yml`

2. **Запуск:**
```bash
cd docling_exp
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка:**
```bash
docker-compose down
```

**Примечание:** При первом запуске модели нейросетей будут автоматически загружены (может занять 1-5 минут). Модели кэшируются в Docker volume `docling-models-cache` для последующих запусков.

## Конфигурация

Конфигурация выполняется через файл `config.yaml`. Основные параметры:

### Сервер

```yaml
server:
  host: "0.0.0.0"      # Хост для привязки
  port: 8000           # Порт
  workers: 1           # Количество воркеров (для production)
  reload: false        # Автоперезагрузка (только для разработки)
```

### Docling

```yaml
docling:
  max_file_size: 104857600  # Максимальный размер файла (100 MB)
  temp_dir: "/tmp/docling" # Временная директория
  
  converter:
    enable_vlm: false       # Использовать VLM (Visual Language Model, например GraniteDocling)
    vlm_model: "granite_docling"  # Название VLM модели
```

### Форматы вывода

```yaml
output:
  formats:              # Поддерживаемые форматы
    - markdown
    - json
    - html
  default_format: "markdown"
  
  json:
    pretty: true
    include_metadata: true
  
  html:
    include_styles: true
    include_images: true
```

### Модели

```yaml
models:
  cache_dir: null       # null = системный кэш, или путь к директории
  preload: false        # Предзагрузка моделей при старте
```

### Логирование

```yaml
logging:
  level: "INFO"         # DEBUG, INFO, WARNING, ERROR
  format: "json"        # json, text
  file: null            # Путь к файлу логов (null = только консоль)
```

## API Endpoints

### POST /parse
Загрузка и парсинг документа из файла.

**Параметры:**
- `file`: файл для загрузки (multipart/form-data)
- `output_format`: формат вывода (markdown, json, html) - опционально

**Пример:**
```bash
curl -X POST "http://localhost:8000/parse?output_format=markdown" \
  -F "file=@document.pdf"
```

**Ответ:**
```json
{
  "filename": "document.pdf",
  "format": "markdown",
  "content": "# Заголовок\n\nТекст документа...",
  "metadata": {
    "pages": 10
  }
}
```

### POST /parse/url
Парсинг документа по URL.

**Параметры:**
- `url`: URL документа
- `output_format`: формат вывода (markdown, json, html) - опционально

**Пример:**
```bash
curl -X POST "http://localhost:8000/parse/url?url=https://arxiv.org/pdf/2408.09869&output_format=markdown"
```

### GET /health
Проверка состояния сервиса.

**Ответ:**
```json
{
  "status": "healthy"
}
```

### GET /models/status
Проверка статуса загрузки моделей.

**Ответ:**
```json
{
  "status": "loaded",
  "message": "Модели загружены и готовы к использованию"
}
```

### GET /
Информация о сервисе и текущей конфигурации.

## Модули сервиса

### app/main.py
Основной модуль FastAPI приложения:
- Определение эндпоинтов API
- Обработка загрузки файлов
- Интеграция с Docling
- Управление временными файлами

### app/config.py
Модуль конфигурации:
- Загрузка конфигурации из YAML
- Валидация через Pydantic
- Управление настройками сервиса

### run.py
Скрипт запуска сервиса с использованием конфигурации.

## О моделях и лицензиях

### Доступность моделей

**Код Docling:** MIT лицензия - полностью открытый исходный код

**Модели нейросетей:**
- Веса моделей загружаются автоматически из официальных источников (Hugging Face)
- Все модели доступны в открытом доступе
- Модели кэшируются локально после первой загрузки
- Работают полностью локально (без отправки данных в облако)

**Используемые модели:**
- **Heron** - модель понимания структуры документов (layout model)
- **OCR модели** - для распознавания текста в сканированных PDF
- **ASR модели** - для обработки аудио файлов (опционально)
- **GraniteDocling** - Visual Language Model (опционально, для расширенного режима)

### Системные требования

- **Память:** минимум 4 GB RAM (рекомендуется 8+ GB)
- **Диск:** 2-4 GB для моделей
- **GPU:** опционально, ускоряет обработку (поддерживается CUDA)
- **Apple Silicon:** автоматическое MLX-ускорение для GraniteDocling

### Где хранятся модели

**Локальный запуск:**
- Linux/Mac: `~/.cache/docling/` или `~/.cache/huggingface/`
- Windows: `%USERPROFILE%\.cache\docling\` или `%USERPROFILE%\.cache\huggingface\`

**Docker:**
- В volume `docling-models-cache` (монтируется в `/app/models_cache`)

## Интерактивная документация

После запуска доступна по адресу:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

## Версии зависимостей

Все версии зафиксированы в `requirements.txt`:
- `fastapi==0.115.0`
- `uvicorn[standard]==0.32.0`
- `docling==2.63.0`
- `pydantic==2.9.2`
- `pyyaml==6.0.2`
- И другие зависимости с фиксированными версиями

## Разработка

### Локальная разработка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. В `config.yaml` установите `reload: true` для автоперезагрузки

3. Запустите сервис:
```bash
python run.py
```

### Тестирование

```bash
# Проверка здоровья сервиса
curl http://localhost:8000/health

# Проверка статуса моделей
curl http://localhost:8000/models/status

# Тестовый парсинг
curl -X POST "http://localhost:8000/parse/url?url=https://arxiv.org/pdf/2408.09869&output_format=markdown"
```

## Troubleshooting

### Модели не загружаются

- Проверьте доступность интернета (для первой загрузки)
- Проверьте свободное место на диске (нужно 2-4 GB)
- Проверьте права доступа к директории кэша

### Ошибки памяти

- Увеличьте лимит памяти в Docker Compose
- Уменьшите количество воркеров
- Отключите предзагрузку моделей (`preload: false`)

### Медленная обработка

- Используйте GPU если доступно
- Увеличьте количество воркеров (для production)
- Проверьте настройки конвертера в конфиге

## Лицензия

Код сервиса - MIT лицензия. Для лицензий моделей см. документацию Docling.

